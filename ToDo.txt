
1. other search filter like only category , gender, brand based filter need to add (means frontend api call only because backend will use the same api for filters)
2. product description page : UI and add backend
3. make the things responsive (support for multiple devices)

